import { DualLanguageText } from 'src/domain/content/entity/dual-language.entity';

export class ContactUsContent {
  address: DualLanguageText;
  work_hours: DualLanguageText;
  call_numbers: string;
}
